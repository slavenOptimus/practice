function funForMe() {
  var probability1 = 0;
  var total;
  if (probability1 >= 0) {
    let payback = 95;
    window.print(total);
    }
   }
   
   var probability1 = prompt();
   var payback;
   
   switch (probability1) {

    case 50:
      payback = 94;
      console.log( total = 1 / probability1 * payback);
        break;

    case 65:
      payback = 90;
      console.log( total = 1 / probability1 * payback);
        break;
    default:
      ...
   }
